<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |b| %>
      <tr>
        <td><%= b.libraries.first.icon%></td>
        <td><%= b.author.name %></td>
        <td><%= b.created_at %></td>
        <td><%= b.name %></td>
        <td><%= b.amount %></td>
     </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', libraries_path %>
