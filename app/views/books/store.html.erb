<p id="notice"><%= notice %></p>

<h1>Store</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
      <td><%= book.name %></td>
      <td><%= book.amount %></td>
      <td><%= book.created_at %></td>
      <td><%= book.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
